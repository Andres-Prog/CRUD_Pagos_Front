node_modules/

# === Archivos de Entorno Locales ===
# Estos archivos contienen variables de entorno y secretos locales.
# NUNCA deben ser subidos a un repositorio público por seguridad.
.env.local
.env.development.local
.env.production.local
.env.test.local

# === Directorios de Build y Cache de Next.js ===
# La carpeta .next es generada por Next.js al construir ('npm run build') o
# ejecutar en modo desarrollo. Contiene archivos optimizados y cache.
.next/
out/

# === Archivos de Log ===
# Archivos de log generados por npm o la aplicación.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Archivos de Configuración del Editor/IDE ===
# Archivos específicos de configuración de tu entorno de desarrollo.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === Archivos del Sistema Operativo ===
# Archivos generados por el sistema operativo que no son parte del proyecto.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# === Reportes de Pruebas y Cobertura ===
# Archivos generados al ejecutar los tests.
coverage/
.jest-cache

# === Archivos de Producción ===
# Archivos generados durante el build para producción que no son necesarios en el repo.
.production/
.vercel/

